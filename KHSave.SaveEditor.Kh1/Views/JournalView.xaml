<UserControl x:Class="KHSave.SaveEditor.Kh1.Views.JournalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="240" d:DesignWidth="400">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock TextWrapping="Wrap">
            KH1 Final Mix stores the FM-only Ansem Reports as Journal flags separate
            from the normal inventory table. Toggle these entries to keep the visible
            Journal state in sync with the report item.
        </TextBlock>

        <DataGrid
            Grid.Row="2"
            AutoGenerateColumns="False"
            Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}"
            CanUserAddRows="False"
            CanUserDeleteRows="False"
            ItemsSource="{Binding Reports}">
            <DataGrid.Columns>
                <DataGridTextColumn
                    Header="Report"
                    Width="*"
                    IsReadOnly="True"
                    Binding="{Binding Name}"/>
                <DataGridCheckBoxColumn
                    Header="Visible In Journal"
                    Width="Auto"
                    Binding="{Binding IsVisible, UpdateSourceTrigger=PropertyChanged}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
